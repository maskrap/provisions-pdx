<div class="item-tile">
  <img src="{{model.image}}">
  <h3>{{model.name}}</h3>
  <h4>{{model.cost}}</h4>
  <p>{{model.description}}</p>
  <button {{action 'addToCart' item}}>Add to Cart</button>
</div>
